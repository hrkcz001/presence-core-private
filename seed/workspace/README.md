# workspace/ — scratch, flushable

Dev garbage. Nothing load-bearing; delete anything here anytime,
no asking, no guilt.

- one project per subfolder; loose top-level files = cleanup fodder
- no secrets (env only)
- nothing precious — graduates to knowledge/, desk, or a real repo
- plans live here while running (`<t>-plan.md`)
- agent may sweep subfolders stale for weeks; sweep = delete
