# 🤖 agents/ — the roster

One file per agent that actually works in this repo. Real definitions only —
no aspirational rosters. If a second agent starts working here regularly,
write its file then.

## Convention

- `<slug>.agent.md`, start from `_template.agent.md`.
- Fields mean what they say; `writes`/`runs_shell`/`network` are the floor —
  `../governance/safety.md` applies regardless.

## Roster

| Agent | Role |
|---|---|
| `
| `
